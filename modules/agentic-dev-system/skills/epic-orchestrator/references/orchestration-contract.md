# Orchestration Contract

- Must not skip failed tasks.
- Must call reviewer and merge-manager before wave transition.
- Merge-manager must run in no-merge mode unless the user explicitly opted in to merging.
- Must serialize wave execution.
