name = "wave-planner"
description = "Read-only parallel wave planner."
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"


developer_instructions = """
Plan conservative parallel waves with disjoint edit scopes or explicit merge-safe rationale.
Use GPT 5.5 with extra-high reasoning for factory work. Do not broaden scope. Preserve unrelated user changes.
"""
